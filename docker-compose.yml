services:
  alphaweb:
    build: .
    container_name: alphaweb-1001
    user: "1001:1001"
    restart: always
    ports:
      - "5000:80"
    networks:
      - VPS-net

networks:
  VPS-net:
    external: true
